<view class="count-wrapper">
    <text catchtap="onRemoveClick" wx:if="{{count>0||foodCount!==0}}"  class="remove">-</text>
    <text class="count" wx:if="{{count>0||foodCount!==0}}">{{count}}</text>
    <text catchtap="onAddClick" class="add">+</text>
  </view>